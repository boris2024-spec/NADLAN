# Обновление API улиц - Использование правильного endpoint (v2)

## 🔄 Что изменилось (ФИНАЛЬНОЕ ОБНОВЛЕНИЕ)

Обновлен resource_id для API улиц на **актуальный** endpoint от data.gov.il.

### История изменений:

**v1 (старый - не работал):**
```javascript
const STREETS_RESOURCE_ID = 'e3674e3f-7f66-4b3b-a0c4-859b5d44b58e';
```

**v2 (ТЕКУЩИЙ - РАБОТАЕТ):**
```javascript
const STREETS_RESOURCE_ID = '9ad3862c-8391-4b2f-84a4-2d4c68625f4b';
```

## 🔗 Правильные API Endpoints

### Получение городов:
```
https://data.gov.il/api/3/action/datastore_search
  ?resource_id=5c78e9fa-c2e2-4771-93ff-7f400a12f7ba
  &limit=1500
```

### Получение улиц города (АКТУАЛЬНЫЙ):
```
https://data.gov.il/api/3/action/datastore_search
  ?resource_id=9ad3862c-8391-4b2f-84a4-2d4c68625f4b
  &filters={"שם_ישוב":"תל אביב - יפו"}
  &limit=5000
```

**Источник:** https://data.gov.il/dataset/321

## 🧪 Тестирование

### Проверка в браузере:
Откройте `test-streets-api.html` и протестируйте:

1. **Тест городов** - должен показать все города Израиля
2. **Тест улиц** - введите "תל אביב - יפו", должны появиться улицы Тель-Авива
3. **Поиск улицы** - введите "תל אביב - יפו" и "דיזנגוף", должна найтись улица

### Прямая проверка API:

**Города:**
```bash
curl "https://data.gov.il/api/3/action/datastore_search?resource_id=5c78e9fa-c2e2-4771-93ff-7f400a12f7ba&limit=10"
```

**Улицы Тель-Авива:**
```bash
curl "https://data.gov.il/api/3/action/datastore_search?resource_id=e3674e3f-7f66-4b3b-a0c4-859b5d44b58e&filters={\"שם_ישוב\":\"תל אביב - יפו\"}&limit=10"
```

## ✅ Результат

Теперь API улиц работает корректно и возвращает реальный список улиц из правительственной базы данных Израиля.

### Пример ответа (улицы):
```json
{
  "success": true,
  "result": {
    "records": [
      {
        "שם_ישוב": "תל אביב - יפו",
        "שם_רחוב": "דיזנגוף",
        "סמל_ישוב": "5000",
        "סמל_רחוב": "1234"
      },
      {
        "שם_ישוב": "תל אביב - יפו",
        "שם_רחוב": "אלנבי",
        "סמל_ישוב": "5000",
        "סמל_רחוב": "2345"
      }
      // ...
    ]
  }
}
```

## 🚀 Что дальше

Никаких дополнительных действий не требуется. Изменения применяются автоматически при перезагрузке приложения.

```bash
# Если нужно перезапустить
cd nadlan_front
npm run dev
```

Теперь автодополнение улиц работает с правильным API! 🎉
